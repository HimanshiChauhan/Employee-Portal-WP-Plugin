jQuery(function()
{
    jQuery("#formPost").validate(
    {
        submitHandler:function()
        {
            jQuery.post(ajaxurl,
                        {action:"employee_portal_ajax_req",
                         client_id:$("#id").val(),
                         client_secret:$("#secret").val(),
                        },
                        function($response)
                        {
                            alert("Your credencials have been successfully stored");
                        }
            );
        }
    });
});
